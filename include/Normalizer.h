#pragma once

#include <vector>

#include <unordered_map>

#include "Token.h"

class Normalizer
{

public:

    std::vector<Token>

    normalize(

        const std::vector<Token>& tokens

    );

};